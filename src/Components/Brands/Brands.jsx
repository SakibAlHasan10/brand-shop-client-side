import Logo from "./Logo/Logo";

const Brands = () => {
  
//   brands.forEach((bra) => {
//     if (!brandName.includes(bra.brand)) {
//       setBrandName([...brandName, bra.brand]);
//     }
//   });
//   console.log(brandName);
  return (
    <div>
      <Logo></Logo>
    </div>
  );
};

export default Brands;
